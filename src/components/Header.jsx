import React  from "react";//important

const Header = () => { //Function name must be same as file name
    return(//we can write html inside return 
    <div>Header</div>
    )
}
export default Header;
 // we are exporting to use in diff files.

// const Footer=() => {
//     return (
//       <div>Footer</div>
//     )
//   }
  
//   export {Header,Footer};